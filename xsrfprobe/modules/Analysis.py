#!/usr/bin/env python3
# -*- coding: utf-8 -*-

# -:-:-:-:-:-:-::-:-:#
#    XSRF Probe     #
# -:-:-:-:-:-:-::-:-:#

# Author: 0xInfection
# This module requires XSRFProbe
# https://github.com/0xInfection/XSRFProbe

from rapidfuzz.distance import Levenshtein
import itertools
import logging

from xsrfprobe.modules.Entropy import calcEntropy
from xsrfprobe.core.utils import sameSequence
from xsrfprobe.files import discovered
from xsrfprobe.core.logger import VulnLogger, NovulLogger

logger = logging.getLogger("Analysis")

def Analysis():
    """
    Observe and analyse the patterns in which CSRF tokens
    are generated by the server.
    """
    # Draw from both the active token list and the passively harvested sample
    # pool so the predictability analysis sees every distinct token observed
    # during the run, not just the one captured from the form under test.
    all_tokens = list(discovered.ANTI_CSRF_TOKENS) + list(discovered.TOKEN_SAMPLES)
    if len(all_tokens) <= 1:
        return

    # Group DISTINCT token values by generator (the token field name). The
    # predictability analysis is only meaningful when comparing two tokens that
    # were *independently generated* by the same generator. Comparing a token
    # against itself — or against an identical value recorded from a different
    # discovery_part (e.g. the same double-submit token seen in both the body
    # and the cookie) — trivially yields edit distance 0 and produces a false
    # "tokens can be forged" verdict. So we dedupe by value and only compare
    # distinct samples sharing the same field name.
    by_name: dict[str, list[str]] = {}
    for tk in all_tokens:
        if not tk.token:
            continue
        values = by_name.setdefault(tk.name, [])
        if tk.token not in values:
            values.append(tk.token)

    comparable = {name: vals for name, vals in by_name.items() if len(vals) >= 2}

    logger.info("+--------------+")
    logger.info("|   Analysis   |")
    logger.info("+--------------+")
    logger.info("Proceeding with post-scan analysis of tokens gathered...")
    logger.info("A total of %s token record(s) across %s distinct value(s) were discovered",
                len(all_tokens), sum(len(v) for v in by_name.values()))

    if not comparable:
        logger.info(
            "Insufficient distinct token samples for predictability analysis "
            "(need >=2 distinct values generated by the same token field)."
        )
        return

    # Emit ONE consolidated, metric-backed verdict per token field instead of a
    # line per pairwise comparison (which flooded the report with identical
    # "cannot be forged" entries).
    for name, values in comparable.items():
        n_samples = len(values)
        lengths = {len(v) for v in values}
        entropies = [calcEntropy(v) for v in values]
        avg_entropy = sum(entropies) / len(entropies)

        dists = [Levenshtein.normalized_distance(a, b)
                 for a, b in itertools.combinations(values, 2)]
        min_dist = min(dists)
        avg_dist = sum(dists) / len(dists)
        max_dist = max(dists)

        # Longest common substring shared by ALL samples = the static component.
        static = values[0]
        for v in values[1:]:
            static = sameSequence(static, v)
            if not static:
                break
        static_len = len(static)
        avg_len = sum(len(v) for v in values) / n_samples
        dynamic_len = avg_len - static_len

        length_desc = (f"{next(iter(lengths))} chars" if len(lengths) == 1
                       else f"{min(lengths)}-{max(lengths)} chars (varies)")

        detail = (
            f"[i] Field: {name}\n"
            f"[i] Distinct samples analysed: {n_samples}\n"
            f"[i] Token length: {length_desc}\n"
            f"[i] Avg Shannon entropy: {avg_entropy:.2f} bits/char\n"
            f"[i] Pairwise edit distance (normalised): min {min_dist:.2f}, avg {avg_dist:.2f}\n"
            f"[i] Shared static component: '{static}' ({static_len} chars)\n"
            f"[i] Avg dynamic length: {dynamic_len:.1f} chars"
        )

        # Verbose per-field breakdown (visible at -v / INFO).
        logger.info("=" * 60)
        logger.info("[Analysis] Token field '%s' - %s distinct sample(s):", name, n_samples)
        for i, (val, ent) in enumerate(zip(values, entropies), 1):
            logger.info("    #%d  value=%s", i, val)
            logger.info("        length=%d chars | Shannon entropy=%.3f bits/char (%.1f bits total)",
                        len(val), ent, ent * len(val))
        logger.info("[Analysis] Length: %s | avg entropy: %.3f bits/char",
                    length_desc, avg_entropy)
        logger.info("[Analysis] Pairwise normalised edit distance over %d pair(s): "
                    "min=%.3f, avg=%.3f, max=%.3f (1.0 = completely different)",
                    len(dists), min_dist, avg_dist, max_dist)
        if static_len:
            logger.info("[Analysis] Shared static component across all samples: '%s' (%d chars)",
                        static, static_len)
        else:
            logger.info("[Analysis] No static component shared across all samples.")
        logger.info("[Analysis] Avg dynamic (varying) length: ~%.1f chars", dynamic_len)
        logger.info("[Analysis] Heuristic thresholds: min edit distance >= 0.50 "
                    "AND dynamic length > 6 chars => strong.")

        # Weak if independently generated tokens are too similar (low inter-token
        # variance) or the varying portion is small enough to brute-force.
        if min_dist < 0.5 or dynamic_len <= 6:
            reasons = []
            if min_dist < 0.5:
                reasons.append(f"min edit distance {min_dist:.2f} < 0.50 (tokens too similar)")
            if dynamic_len <= 6:
                reasons.append(f"dynamic component ~{dynamic_len:.0f} chars <= 6 (brute-forceable)")
            logger.warning("[Analysis] Token field '%s' looks predictable/forgeable: %s.",
                           name, "; ".join(reasons))
            VulnLogger(
                "Analysis",
                f"Anti-CSRF token field '{name}' appears weak/forgeable: low inter-token "
                f"variance (min normalised edit distance {min_dist:.2f}) or small dynamic "
                f"component (~{dynamic_len:.0f} chars).",
                detail,
                test_id="A1",
            )
        else:
            logger.info("[Analysis] Token field '%s' appears strong: min edit distance "
                        "%.2f >= 0.50 and dynamic component ~%.0f chars > 6.",
                        name, min_dist, dynamic_len)
            NovulLogger(
                "Analysis",
                f"Anti-CSRF token field '{name}' appears strong: {n_samples} distinct "
                f"samples, avg entropy {avg_entropy:.2f} bits/char, min normalised edit "
                f"distance {min_dist:.2f}, dynamic component ~{dynamic_len:.0f} chars.",
                test_id="A1",
            )

    logger.info("Post-Scan Analysis Completed!")
